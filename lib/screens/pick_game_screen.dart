import 'package:flutter/material.dart';

class PickGamesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('PICK GAMES SCREEN'));
  }
}
