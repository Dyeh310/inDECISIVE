import 'package:flutter/material.dart';

class GamesInProgressScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('GAMES IN PROGRESS'));
  }
}
